# BackTestingEngine3min
Streamlit app to view scrip CSV candle data from `Input Files`, load and save trade signals into matching CSVs in `Output Files`, display a 3-minute chart with EMA and markers, filter by date, and manage trades in the `Saved Signals` table.
